import { Form,Input,InputNumber ,Button} from "antd";

const layout = {
  labelCol: {
    // span: 4,
  },
  wrapperCol: {
    // span: 4,
  },
  maxWidth:{
  	// maxWidth:30
  }
};

function Other() {

	return (
		<div>
		 	程序部署中
		</div>
	)
}

export default Other

